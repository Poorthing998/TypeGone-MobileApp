<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/keyboard_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#1C1C1E"
    android:orientation="vertical"
    android:paddingBottom="16dp">

    <!-- Voice Bar -->
    <LinearLayout
        android:id="@+id/voiceBar"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:background="#2C2C2E"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="8dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="⟶  Tap MIC to voice-type"
            android:textColor="#B0B0B5"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"/>

        <!-- Mode button (TextView, not Material Button) -->
        <TextView
            android:id="@+id/shortcutButton"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="Mode"
            android:textSize="10sp"
            android:textColor="#FFFFFF"
            android:background="@drawable/mode_btn_dark"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:layout_marginEnd="6dp"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"/>

        <!-- MIC button (TextView, not Material Button) -->
        <TextView
            android:id="@+id/recordButton"
            android:layout_width="wrap_content"
            android:layout_height="34dp"
            android:text="🎙 MIC"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#1C1C1E"
            android:background="@drawable/mic_btn_dark"
            android:paddingStart="18dp"
            android:paddingEnd="18dp"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"/>

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#3A3A3C"/>

    <com.typegone.mobile.TypeGoneKeyboardView
        android:id="@+id/keyboard_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:keyPreviewLayout="@layout/key_preview"
        android:background="#1C1C1E"
        android:keyBackground="@drawable/key_bg_dark"
        android:keyTextColor="#FFFFFF"
        android:keyTextSize="16sp"
        android:shadowColor="#00000000"
        android:shadowRadius="0"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"/>

</LinearLayout>
